# GitHub status

| Item | Status |
|------|--------|
| Repo | `bitplorer/cek-python` |
| Packages | `cek-host`, `cek-surface` on tree |
| CI | `.github/workflows/test.yml` |
| Install | `pip install -e ./cek-host -e ./cek-surface` |
| Kernel | `CekHostPyKernel` when `cek_host` on path |

### 2026-08-15 sync

- Added `docs/PERFORMANCE.md` and `docs/performance-report.json` (real-world + load)
- LATENCY / SHIPPED / ROADMAP cross-linked to measured numbers
