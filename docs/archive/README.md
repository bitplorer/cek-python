# Archive

These pages are the record of earlier drafts. They keep the words the code used then.

Current names, if you are reading an old sentence:

| Old word in these pages | Current name |
|---|---|
| `S`, `LEGAL_PAIRS`, `is_legal` | declared catalog, `CATALOG_PAIRS`, `in_catalog` |
| `chrome` (Host → Peer message) | `perception` |
| taught / leftover peer | in-process peer / subprocess `cek apply` |
| `cek_host.legal` | `cek_host.catalog` |
| `Host.adapt`, `Host.lab` | `Host(mode="production")` or `Host.production()` |
| `doctor` | `check` |
| `Host.demo`, `mode="demo"`, `DEMO_SECRET` | `Host.dev`, `mode="dev"`, `DEV_SECRET` |
